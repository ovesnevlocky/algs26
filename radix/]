#include <stdio.h>
#include <stdlib.h>


int findBiggestNum(int a)
{
	int ret = 0;
	while(a > 0)
	{
		a = a/10;
		ret++;
	}

	return ret;
}


int main()
{
	int size = 10; 
	int tmp = 0;

	int sorted[size];
	int **arrForSorting = malloc(sizeof(int*) * 10);



	for(int i = 0;  i < 10; i++)
		arrForSorting[i] = malloc(sizeof(int)* size);

	int *tmp_arr = NULL; 
	for(int i = 0; i < size; i++)
	{
		tmp_arr = arrForSorting[i];
		for(int j = 0; j < size; j++)
		{
			tmp_arr[j] = rand() % 1223;		
		}
		tmp_arr = NULL;

	}

	int biggestNum = 0;
	for(int i = 0; i < size; i++)
	{
		tmp_arr = arrForSorting[i];
		for(int j = 0; j < size; j++)
		{
			tmp = findBiggestNum(tmp_arr[j]);
			if(tmp > biggestNum)
				biggestNum = tmp;

		}
		putchar('\n');
		tmp_arr = NULL;

	}
		


	for(int i = 0; i < size; i++)
	{
		tmp_arr = arrForSorting[i];
		for(int j = 0; j < size; j++)
		{
			printf("%i ", tmp_arr[j]);		
		}
		putchar('\n');
		tmp_arr = NULL;

	}
		
	putchar('\n');	
}
